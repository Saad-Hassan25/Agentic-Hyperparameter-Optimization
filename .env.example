# Copy to .env and fill in. The .env file is loaded automatically.

# Optional — omit entirely to run in --no-llm mode (baseline sampler only, no
# network calls ever attempted). Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# Any model on OpenRouter that returns JSON reliably. Examples:
#   anthropic/claude-sonnet-4
#   anthropic/claude-3.7-sonnet
#   google/gemini-2.5-pro
#   openai/gpt-4o-mini
OPENROUTER_MODEL=anthropic/claude-sonnet-4

# Optional — defaults shown
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional — attribution shown on OpenRouter's dashboards
# OPENROUTER_SITE_URL=https://your-site.example
# OPENROUTER_APP_NAME=hp-agent
